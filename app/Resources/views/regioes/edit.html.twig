{% extends 'base.html.twig' %}

{% block body %}
    <h1>Editar Região</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Salvar" class="btn btn-success" />
        <a href="{{ path('regioes_index') }}"  class="btn btn-danger"> Cancelar</a>
    {{ form_end(edit_form) }}   
{% endblock %}
