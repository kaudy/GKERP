{% extends 'base.html.twig' %}

{% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}

{% block body %}
    <h1>Contatosfornecedore creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
            <input type="submit" value="Salvar" class="btn btn-success" />
            <a href="{{ path('fornecedores_index') }}"  class="btn btn-danger"> Cancelar</a>
    {{ form_end(form) }}    
{% endblock %}
