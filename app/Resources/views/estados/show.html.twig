{% extends 'base.html.twig' %}

{% block body %}
    <h1>Estado</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estado.id }}</td>
            </tr>
            <tr>
                <th>Idregiao</th>
                <td>{{ estado.idRegiao }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ estado.estado }}</td>
            </tr>
            <tr>
                <th>Uf</th>
                <td>{{ estado.uf }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('estados_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('estados_edit', { 'id': estado.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
