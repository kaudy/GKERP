{% extends 'base.html.twig' %}

{% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}

{% block body %}
    <h2>Cadastrar Novo Tipo de Cidade</h2>
    <div class="col-md-8">
        <br>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Salvar" class="btn btn-success" />
            <a href="{{ path('tiposcidades_index') }}"  class="btn btn-danger"> Cancelar</a>
        {{ form_end(form) }}
    </div>    
{% endblock %}